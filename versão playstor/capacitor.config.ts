import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.albermidia.burguersprime',
  appName: 'BURGUERSPRIME',
  webDir: 'dist'
};

export default config;
